---
- name: Deploy app from Git for Pathnex
  hosts: all
  become: yes


  tasks:
   - name: Install giut
     yum:
      name: git 
      state: present


    - name: Clone repository
      git:
       repo: "https://github.com/pathnex/app.git"
       dest: "opt/patghnex-app"




resource "aws_db_instance" "PathnexRDS" {
    allocated_storage   = 20
    engine   = "mysql"
    instance_class  =  "db.t3.micro"
    username   = "admin"
    password  = "Pathnex123"
    skip_final_snapshot  = true
}       



apiVersion: apps/v1
kind: Deployment
metadata:
 name: pathnex-rollout

spec:
replicas: 3
startegy:
 type: RollingUpdate
 rollingUpdate:
  maxSurge: 2
  minUnavailable: 1

selector:
  matchLabels:
   app: rollout

  template:
   metadata:
    labels:
     app: rollout

   spec:
    containers:
     - name: app
       image: nginx





#!/bin/bash

ping -c 2 google.com &> /dev/null

if [$? -eq 0]; then
 echo "Internet is available"

 else
   echo "No internet connection"
 fi 



 pipeline {
    agents any 
    environment {
        INSTITUTE_NAME ="Pathnex"
    }
    stages{
        stage('Deploy') {
            steps{
                input message: "Approve deployment for $INSTITIUTE_NAME?"
                sh 'echo "Deployment approved and executed!"'

            }
        }
    }
 }  





stages:
 - deploy 

variables:
  INSTITUTE_NAME: "Pathnex"

deploy:
 stages: deploy 
 image: alphine:latest
 scripts:
  - echo "Ready to deploy $INSTITUTE_NAME"

when: manual


